<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/kit_bg_white_ripple"
    android:paddingHorizontal="@dimen/chat_dp12">

    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/chat_dp40"
        android:layout_height="@dimen/chat_dp40"
        android:layout_marginTop="@dimen/chat_dp8"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@mipmap/kit_ic_default_avatar" />

    <TextView
        android:id="@+id/name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/chat_dp12"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/chat_skin_Text"
        android:textSize="@dimen/chat_dp15"
        app:layout_constraintBottom_toBottomOf="@id/image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/image"
        app:layout_constraintTop_toTopOf="@id/image"
        tools:text="会飞的牛肉干" />

    <View
        android:layout_width="0dp"
        android:layout_height="@dimen/chat_dp0_8"
        android:layout_marginTop="@dimen/chat_dp8"
        android:alpha="0.5"
        android:background="@color/chat_skin_line"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/name"
        app:layout_constraintTop_toBottomOf="@id/image" />

</androidx.constraintlayout.widget.ConstraintLayout>