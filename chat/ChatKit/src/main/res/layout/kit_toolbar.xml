<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="@dimen/chat_dp55"
    android:orientation="vertical"
    tools:background="@color/chat_conversation_toolbar"
    tools:viewBindingIgnore="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/chat_conversation_toolbar_title"
            android:textSize="@dimen/chat_dp16"
            tools:text="张三" />

        <ImageView
            android:id="@+id/left"
            android:layout_width="@dimen/chat_dp55"
            android:layout_height="match_parent"
            android:padding="@dimen/chat_dp14"
            android:src="@drawable/chat_arrow_left_white" />
    </RelativeLayout>

</LinearLayout>